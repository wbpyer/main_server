3.11
数据库端加入了发的相关功能，并作出了改动。
docker 的更改方式，直接修改，然后重启动
关于访问的时候也做了更改，可以看发文件的内容，接口也都做了更新。如果确定没问题，只需要push一下就可以了


3.23主服务更改
1,给王倩发送数据，多家了一个功能，发到redis8好库中这个是给王倩用的。
2,增加了数据库字段，变成了按照业务分配虚拟机的模式。预留了很多备用字段
3,主服务加了判断，根据业务或者是岗位来区分，如果是工区就用岗位来区分，如果是项目部或者公司，目前就用业务。这是
现在主服务调度虚拟机的逻辑。

4.3数据库端改动。
目前看这次库端的改动非常小，问题不是很大。
2处改动。都已经做了todo，线上版本也做了更新。

4.3main_ser 调度端也要改动，接下来改调度端。
已经改动完毕，目前就是用IP地址来判断机器，线上版本已经更新


4.4数据库端改动。
为了工区能够实现报送的功能，加入一个表，这个表里面是可以变动的数据库业务表，
也就是说，每次部门长动了权限配置之后，都需要动这个表，这个接口，已经为张敏准备好。
增加findname 接口， 用来给虚拟机端使用。
改动完毕，明天测试。本地测试。联调。

4.5
调度端，已经更新完毕，
数据库端更新完毕，

4.6 数据库端，和调度端部署昭通完毕。
对现有代码做整合go
目前db是最新功能完整版本，截止到4.6
目前main，也是最新功能完整版本，线上发布版本。


















